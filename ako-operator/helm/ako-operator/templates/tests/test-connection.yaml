apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ako-operator.fullname" . }}-test-connection"
  labels:
    {{- include "ako-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ako-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
